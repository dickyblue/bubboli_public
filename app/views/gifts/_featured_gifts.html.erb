
<div class="gallery root-shadow-effect">

  <ul class="slideshow">
      <%# @rotating_gift_images.each do |fav|%>
      <li>
          <%#= link_to image_tag(fav.gift_images.last.image_url(:ex_large_pic), :title => fav.name, :alt => truncate(fav.description, :length => 130) ), fav %>
      </li>
      <%# end %>
  </ul>
</div>

<br>



